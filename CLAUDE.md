# 이 저장소에서 일하는 법

**새 세션은 이 파일을 먼저 읽는다.** 여러 대의 PC에서, 여러 세션이 병렬로 도는 저장소다.
세션 기록은 PC 밖으로 안 나간다 — **커밋된 파일만 넘어간다.**

배경·설계 근거는 [`README.md`](README.md)에 있다. 여기 있는 건 *작업 규율*이다.

---

## 무엇을 만드는가

같은 앨범이라도 판매처마다 특전 포토카드가 다르다. 그걸 자동으로 모아 비교 페이지를 만든다.

```
컴백 탐지(위버스샵) → 판매처 7곳 수집 → 3축 정규화(이벤트·에디션·포장)
→ 배송비·조합 최적화 → out/album/{slug}.html
```

```bash
node build.mjs                 # 전체 (예판 중인 앨범 전부, 5~8분)
node build.mjs TAEMIN          # 앨범 하나만 (부분 빌드 — 인덱스는 지난 결과에 머지)
node build.mjs --max=3         # 개수 제한
node watch.mjs                 # 새로 뜬 것만 확인 (3초)
```

배포는 https://kpop-album.vercel.app — Vercel은 `out/`을 서빙만 하고, 빌드는
GitHub Actions에서 돈다(판매처가 클라우드 IP를 막을 수 있어서).

---

## 절대 규칙

이 넷은 전부 **실제로 틀려서 되돌린 적이 있는 것**들이다. 각 사고 기록은 커밋과 `docs/`에 있다.

**1. 대리 지표로 내용을 판단하지 마라.**
이미지 비율 2.4를 기준으로 "이건 공지 배너다"라고 판정했다가 특전 이미지 37건 중
30건을 지웠고 그 상태로 라이브에 배포됐다(`54bf816` → `b39d914`로 되돌림).
치수·길이·이름 패턴은 내용이 아니다. **파일을 열어보고 판단하라.**

**2. 못 찾은 것을 "없다"로 단정하지 마라.**
파싱 실패와 "특전이 실제로 없음"은 다른 사실이다. 특전 상태는 넷으로 구분한다 —
내용 표시 / `특전 없음`(위버스샵 icons로 단정 가능) / `특전 있음·구성 비공개` / `확인 못함`.
`—` 하나로 뭉개면 사용자에게 거짓말이 된다.

**3. 입력을 바꿔도 안 변하는 응답은 신호가 아니다.**
예스24 검색이 200에 507KB를 돌려줘서 "열린다"고 판정했는데, 쿼리를 뭘 넣어도
결과가 동일했다. 새 판매처를 붙일 땐 **쿼리 두 개를 넣어 응답이 달라지는지부터** 본다.

**4. 값이 여러 개면 어느 걸 집는지 확인하라.**
목록 카드에 `16,000원 13,800원 14%`처럼 정가와 판매가가 같이 나온다. 정가를 집어서
"위드뮤만 비싸다"는 틀린 결론을 문서에까지 적었었다. 뮤직플랜트도 금액이 셋이다
(discount·sell·consumer).

**커밋 메시지는 한국어로, 무엇을 왜 바꿨는지 한 줄.** 기존 로그 형식을 따른다.

---

## 레인 — 파일 소유권

세션 여러 개가 동시에 돌 때 **이 경계를 넘지 않는다.** 넘어야 할 일이 생기면
직접 고치지 말고 `docs/`에 인계 문서를 남긴다 (`docs/38-수집단계-인계.md`가 그 예다).

| 레인 | 소유 | 금지 |
|---|---|---|
| **프론트** | `src/render.mjs` · `src/ogcard.mjs` · `src/seo.mjs` | `src/` 수집기, `build.mjs` |
| **코드** | `src/` 수집기 전부 · `build.mjs` · `watch.mjs` · `api/` · `.github/` | **`src/render.mjs`** |
| **홍보** | `docs/` · `README.md` | 코드 전부 |

프론트↔코드 경계는 이미 한 번 돌려본 방식이다 — `docs/38-수집단계-인계.md`가
"프론트에서 못 고치는 것"을 프롬프트째로 수집 레인에 넘긴 문서다.

---

## 재빌드는 **한 번만** 건다 — 이미 대기 중이면 아무것도 안 바뀐다

레인 표는 파일 소유권만 정하고 워크플로 트리거는 안 정한다. 정할 필요가 없다 —
`rebuild.yml`이 이미 막고 있다.

```yaml
concurrency:
  group: site-build
  cancel-in-progress: false
```

2026-08-28에 한 시간 동안 rebuild가 8번 걸렸고 3번이 `cancelled`로 끝났다.
그런데 **취소된 3건은 job을 하나도 시작하지 않았다**(`gh run view --json jobs` → `[]`).
빌드 시간은 1분도 안 탔다. `cancel-in-progress: false`가 하는 일이 정확히 이거다 —
**도는 것은 지키고, 큐에 쌓인 것 중 제일 최신 하나만 남긴다.**

그리고 `workflow_dispatch`는 **거는 시점의 origin/main**을 빌드한다. 그래서 나중에 건
것은 먼저 건 것의 커밋을 항상 포함한다. 실측(`fb17139` ⊂ `5a721ce`)으로도 맞았다.
**살아남은 run이 언제나 가장 많은 커밋을 갖는다** — 내 수정이 빠진 채 배포될 일은 없다.

실제 비용은 **배포 지연** 하나다. 지금 도는 빌드(8~9분)가 끝나야 큐가 풀린다.
거기에 또 걸어봐야 대기열만 갈아치우고 시작 시각은 그대로다.

**그래서 규칙은 "누가 누르나"가 아니라 "겹쳐 누르지 마라"다.**

보는 건 "뭔가 돌고 있나"가 아니라 **"큐에 있는 게 내 커밋을 포함하나"**다.
run은 걸린 시점의 SHA에 **고정된다** — 내 푸시보다 먼저 걸린 run은 나중에 내가
푸시해도 옛 커밋을 빌드한다.

```bash
git push
gh run list --workflow=rebuild.yml --limit 3 --json databaseId,status,headSha \
  --jq '.[] | "\(.databaseId) \(.status) \(.headSha[0:7])"'

# 대기/진행 중인 run의 headSha가 내 커밋을 포함하면 → 걸지 않는다
git merge-base --is-ancestor $(git rev-parse HEAD) <그 headSha> && echo 포함

# 포함 안 하면(= 내 푸시보다 먼저 걸린 run이면) 한 번 건다.
# 큐에 있던 옛 run은 자동으로 잘린다 — job을 시작 안 했으니 버리는 것도 없다
gh workflow run rebuild.yml
```

푸시만으로는 사이트가 안 바뀐다(Vercel은 `out/`을 서빙만 한다). **푸시 뒤에 걸린
재빌드에만 올라탄다.** 하루 2회 자동(KST 10시·19시)도 같다.

---

## `out/`은 커밋하지 않는다 — 병렬에서 제일 먼저 터지는 곳

`out/`의 73개 파일이 git에 추적된다. 그런데 **`node build.mjs`는 그 73개를 전부 다시 쓴다.**
세션 셋이 각자 빌드하면 셋 다 73개 파일이 수정된 상태가 되고, 충돌이 확정이다.

**작업 세션은 `out/`을 커밋하지 않는다.** 소스만 커밋한다.

```bash
git add src/ docs/ build.mjs   # 명시적으로. `git add -A` 쓰지 마라 — out/이 딸려온다
git status --short out/        # 커밋 전에 확인
git checkout -- out/           # 로컬 빌드 산출물 버리기
```

`out/`을 갱신하는 건 `.github/workflows/rebuild.yml`뿐이다(하루 2회 KST 10시·19시,
`rebuild: <시각>` 커밋). 로컬 빌드는 **눈으로 확인하는 용도**이고 결과물은 버린다.

예외 — 렌더 변경을 지금 당장 라이브에 올려야 할 때만 `out/`을 같이 커밋한다.
그때는 **다른 레인이 커밋을 멈춘 상태여야 한다.**

---

## PC를 옮길 때 / 세션을 닫을 때

세션 컨텍스트는 `~/.claude/projects/`에만 쌓이고 **PC를 넘어가지 않는다.**
보조 PC에서 이어 하려면 남길 것을 커밋해야 한다.

레인을 마칠 때 (또는 자리를 뜰 때):

1. **푸시한다.** 안 하면 다른 PC에서 존재하지 않는 작업이다
2. 안 끝난 게 있으면 `docs/`에 남긴다 — **무엇을 시도했고, 뭐가 반증됐고, 다음이 뭔지**.
   코드만 보면 안 보이는 것들이다
3. 되돌린 판단이 있으면 **왜 틀렸는지까지** 적는다. `docs/38`의 2번 항목이 그 형식이다

보조 PC에서 시작할 때:

```bash
git pull
git log --oneline -20          # 세 레인이 뭘 했는지
ls docs/                        # 인계 문서가 늘었는지
```

---

## 비밀·환경변수

`vapid-keys.txt`와 `.env*`는 `.gitignore`에 있다. **절대 커밋하지 않는다.**

빌드에 필요한 것 — GitHub Variables의 `SITE_URL`(없으면 canonical·og:url·sitemap을
통째로 생략한다. 틀린 도메인을 박는 것보다 나아서 그렇게 설계했다), `VAPID_PUBLIC_KEY`.
Vercel 쪽은 Upstash Redis 연결과 `VAPID_*` 환경변수.

`playwright`가 없으면 위드뮤만 조용히 빠지고 나머지는 그대로 돈다 — 로컬에서
`node build.mjs`가 위드뮤 없이 도는 건 정상이다.
